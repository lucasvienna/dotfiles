# Shared alacritty config. Machine-specific bits (font family/size, window
# title) belong in local.toml, which isn't tracked by git.
#
# theme.toml is a symlink managed by `dot-theme-set`, pointing at
# themes/${THEME}/alacritty.toml. Since live_config_reload is on, switching
# themes repaints running terminals with no signal and no restart.

[general]
import = ["~/.config/alacritty/theme.toml", "~/.config/alacritty/local.toml"]
live_config_reload = true
working_directory = "None"

# Theme-independent, so it lives here rather than in every theme file.
[colors]
draw_bold_text_with_bright_colors = true

[font]
normal = { family = "CaskaydiaCove Nerd Font Mono" }

[env]
TERM = "xterm-256color"
WINIT_X11_SCALE_FACTOR = "1.0"

[window]
dimensions = { columns = 140, lines = 45 }
dynamic_padding = true
decorations = "Full"
decorations_theme_variant = "Dark"
opacity = 1.0
class = { instance = "Alacritty", general = "Alacritty" }

[scrolling]
history = 10000
multiplier = 3

[selection]
semantic_escape_chars = ",│`|:\"' ()[]{}<>\t"
save_to_clipboard = true

[cursor]
style = { shape = "Underline", blinking = "Off" }
unfocused_hollow = true
thickness = 0.15

[mouse]
hide_when_typing = true
bindings = [{ mouse = "Middle", mods = "None", action = "PasteSelection" }]

[keyboard]
bindings = [
  { key = "Paste", mods = "None", action = "Paste" },
  { key = "Copy", mods = "None", action = "Copy" },
  { key = "L", mods = "Control", action = "ClearLogNotice" },
  { key = "L", mods = "Control", mode = "~Vi", chars = "\f" },
  { key = "PageUp", mods = "Shift", mode = "~Alt", action = "ScrollPageUp" },
  { key = "PageDown", mods = "Shift", mode = "~Alt", action = "ScrollPageDown" },
  { key = "Home", mods = "Shift", mode = "~Alt", action = "ScrollToTop" },
  { key = "End", mods = "Shift", mode = "~Alt", action = "ScrollToBottom" },
  { key = "V", mods = "Control|Shift", action = "Paste" },
  { key = "C", mods = "Control|Shift", action = "Copy" },
  { key = "F", mods = "Control|Shift", action = "SearchForward" },
  { key = "B", mods = "Control|Shift", action = "SearchBackward" },
  { key = "C", mods = "Control|Shift", mode = "Vi", action = "ClearSelection" },
  { key = "Key0", mods = "Control", action = "ResetFontSize" },
  # ESC + CR, so TUIs can tell Shift+Enter apart from Enter.
  { key = "Return", mods = "Shift", chars = "\u001B\r" },
  { key = "N", mods = "Super", action = "CreateNewWindow" },
]
