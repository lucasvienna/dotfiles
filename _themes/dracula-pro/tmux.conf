set -g mode-style "fg={{Purple}},bg={{Selection}}"

set -g pane-border-style "fg={{Selection}}"
set -g pane-active-border-style "fg={{Comment}}"

set -g status-style "fg={{Purple}},bg={{Background}}"
set -g window-status-style "fg={{Comment}},bg={{Background}}"
set -g window-status-current-style "fg={{Purple}},bg={{Background}}"
